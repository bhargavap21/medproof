pragma language_version 0.15;

import CompactStandardLibrary;

// Minimal test with just a ledger and circuit
export ledger proofCount: Counter;

export circuit incrementProof(): [] {
  proofCount.increment(1);
}

